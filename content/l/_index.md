---
title: "Short links"
# The /l/ section is a redirect namespace, not a browsable page. Individual
# /l/<slug>/ pages are generated by _content.gotmpl; the section index itself
# must never render. The human-readable list lives at /go/links/.
build:
  render: never
  list: never
sitemap:
  disable: true
---
